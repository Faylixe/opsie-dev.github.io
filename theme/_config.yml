exclude:
  - Gemfile
  - Gemfile.lock
  - opsie.gemspec

markdown      : kramdown
highlighter   : rouge
permalink     : date
paginate      : 8
paginate_path : /page:num

plugins:
  - jekyll-feed
  - jekyll-paginate
  - jekyll-sitemap
  - jemoji

sass:
  sass_dir   : _sass
  style      : compressed
